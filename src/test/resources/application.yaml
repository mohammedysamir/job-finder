spring:
  #default spring security configuration
  security:
    user:
      name: user
      password: password
      roles: USER

  #redis configuration
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000
      database: 0
      enabled: false
  #RabbitMQ configuration
  rabbitmq:
    host: localhost
    port: 15672
    username: jobfinder
    password: PeFwrDFD4gKZvTzxcvZf
    virtual-host: /
    publisher-confirms: true
    publisher-returns: true
    enabled: false

    #SMTP configuration
    mail:
      host: smtp.example.com
      port: 587
      username: mohammedre4a@gmail.com
      password: EXoCguG3$ip!WP
      from: mohammedre4a@gmail.com
      properties:
        mail:
          smtp:
            auth: true
            starttls:
            enable: true
  #Database configuration
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
    h2:
      console:
        enabled: true

#Actuator info configuration
management:
  endpoints:
    web:
      exposure:
        include: "info,health,metrics"
  endpoint:
    health:
      show-details: always
    info:
      git:
        enabled: true
